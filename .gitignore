# Sensitive data — never commit (handled by scripts/data/.gitignore)
scripts/data/
scripts/docker/data/

# Generated by generate-home-config.sh — populated from scripts/docker/data/
scripts/docker/home-config/ssh/vpn.cfg
scripts/docker/home-config/ssh/authorized_keys

# Docker submodule — managed separately
/docker/

# Cloned repos (legacy)
luluman_docker/

# Sisyphus working directory
.sisyphus

# OpenCode working directory
.omo/

# OpenCode runtime state (auto-generated, contains machine-specific absolute paths)
.opencode/state/

# Data directory (sensitive configs, generated content)
data/
!scripts/data/
!scripts/data/*.example

# Gitea — 仓库代码、数据库、配置等数据，不应提交到 Git
scripts/gitea/data/

# macOS
.DS_Store

# Python
__pycache__/
*.py[cod]
.venv/

# Sensitive data — never commit
data/

# Generated by auto_script.sh—these are populated from data/
home-config/ssh/vpn.cfg
home-config/ssh/authorized_keys

# Docker submodule — managed separately
/docker/

# Cloned repos (legacy)
luluman_docker/

# macOS
.DS_Store

# Sensitive data — never commit (handled by scripts/docker/data/.gitignore)
scripts/docker/data/

# Generated by generate-home-config.sh — populated from scripts/docker/data/
scripts/docker/home-config/ssh/vpn.cfg
scripts/docker/home-config/ssh/authorized_keys

# Docker submodule — managed separately
/docker/

# Cloned repos (legacy)
luluman_docker/

# Sisyphus working directory
.sisyphus

# macOS
.DS_Store
